{% block page_content %}
    <button type="button" class="btn btn-primary has-tooltip" data-toggle="modal" data-target="#modal-default" title="Ajouter un pass"><i class="fa fa-plus"></i></button>
    <div class="modal fade modal-pass-new" id="modal-default">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title">Ajouter un pass</h4>
                </div>
                {{ form_start(form, {attr: {id: 'form_new_pass'}}) }}
                    <div class="modal-body">
                        <div class="box-body">
                            <div class="alert ajax-feedback hide"></div>
                            {{ form_widget(form) }}
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Annuler</button>
                        <input type="submit" class="btn btn-primary pull-right" value="Créer" />
                    </div>
                    {{ form_rest(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
