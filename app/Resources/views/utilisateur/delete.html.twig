<button type="button" class="btn btn-danger btn-sm" data-toggle="modal" data-target="#modal-delete{{utilisateur.id }}"><i class="fa fa-remove"></i></button>
<div class="modal " id="modal-delete{{utilisateur.id}}">
    <div class="modal-dialog modal-sm">
        <div class="modal-content">
            <style>
                #modal-delete{{utilisateur.id}} {
                text-align: center;
                padding: 0!important;
                }
                #modal-delete{{utilisateur.id}}:before {
                content: '';
                display: inline-block;
                height: 100%;
                vertical-align: middle;
                margin-right: -4px;
                }
                #modal-delete{{utilisateur.id}} .modal-dialog {
                display: inline-block;
                text-align: left;
                vertical-align: middle;
                }
            </style>
            {{ form_start(form) }}
            <div class="modal-body">
                Souhaitez-vous supprimer définitivement <span style="color: red">{{utilisateur.username }}</span>?
            </div>
            <div class="modal-footer">
                <button class="btn btn-danger">Supprimer</button>
                <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
            </div>
            {{ form_widget(form._token, {id: form._token.vars.id ~ '_delete_' ~ utilisateur.id })}}
            {{ form_end(form, {render_rest:false}) }}
        </div>
        <!-- modal-content -->
    </div>
    <!-- modal-dialog -->
</div>

