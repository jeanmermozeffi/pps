{% extends 'AvanzuAdminThemeBundle:layout:base-layout.html.twig' %}

    {% block page_content %}
     <div class="row">
        <div class="col-md-12">
            <!-- general form elements -->
           
                    <div class="list-group">
                        {% for constante in constantes %}
                            <a href="{{ path('gestion_patientconstante_show', {id: constante.id}) }}" class="list-group-item">{{ constante.libelle }}</a>
                        {% endfor %}
                    </div>
               
        </div>
    </div>
    
    {% endblock %}
{% block page_title %}
        Suivi - Mesure
    {% endblock %}

    {% block page_subtitle %}
    {% endblock %}

