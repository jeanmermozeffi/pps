{% extends 'AvanzuAdminThemeBundle:layout:base-layout.html.twig' %}

{% block page_content %}
    <div class="row">
        <div class="col-md-12">
            <!-- general form elements -->
            <div class="box box-primary">
                <div class="box-header">
                    <!-- Horizontal Form -->
                    <div class="">
                        <div class="box-header with-border">
                            <h3 class="box-title">Liste des sites</h3>
                        </div>
                    </div>
                </div>
                <div class="box-body">
                    <a onclick="return {{ grid.hash }}_reset();" class="btn btn-success"><i class="fa fa-repeat"></i></a>
                    <a class="btn btn-default" data-toggle="modal" data-target="#modal-table"
                            href="{{ path('admin_gestion_site_new', {isAjax: true}) }}"
                           ><i class="fa fa-plus"></i> Nouveau </a>
                    
                    {% include('grid.html.twig') %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts_inline %}
    <script>
    init_select2();
        /*$('#form_new_affection').on('submit', function (e) {
            e.preventDefault();
            const $this = $(this);
            const $ajax_feedback = $('.ajax-feedback', $this);
            $.post($this.attr('action'), $this.serialize(), function (data) {

                if (!data.success) {
                   
                    const errors = data.message;
                    let error_str = '';
                    for (error of errors) {
                        error_str += `<p>${error}</p>`;
                    }
                    
                    $ajax_feedback.removeClass('hide').html(error_str);
                }
            }, 'json');
        }); */
    </script>
{% endblock %}
