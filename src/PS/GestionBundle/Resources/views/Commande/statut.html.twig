{% block page_content %}
    <button type="button" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#modal-statut{{commande.id}}" data-url="{{ form.vars.action }} "><i class="fa fa-refresh"></i></button>
    <div class="modal fade" id="modal-statut{{commande.id}}">

        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title">Statut</h4>
                </div>
                {{ form_start(form, { 'attr': {'class': 'form-horizontal'}}) }}
                   
                    <div class="modal-body">
                        <div class="box-body">
                            {{ form_errors(form) }}
                            <div class="form-group">
                                <label>Corporate</label>
                                <p class="help-block">{{ commande.corporate.raisonSociale }}</p>
                            </div>
                            <div class="form-group">
                                <label for="nom">Nombre de PASS</label>
                                <p class="help-block">{{ commande.qteCommande }}</p>
                            </div>
                            <div class="form-group">
                                <label for="nom">Date</label>
                                <p class="help-block">{{ commande.dateCommande|date('d-m-Y à H:i:s') }}</p>
                            </div>
                            <div class="form-group">
                                <label for="{{ form.statutCommande.vars.id ~ '_' ~  commande.id }}" class="col-sm-4">Statut</label>

                                <div class="col-sm-8">
                                    {{ form_widget(form.statutCommande, {id: form.statutCommande.vars.id ~ '_' ~  commande.id}) }}
                                </div>
                            </div>
                            
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Annuler</button>
                        <input type="submit" class="btn btn-primary pull-right" value="Modifier" />
                    </div>
                    {{ form_widget(form._token, {id: form._token.vars.id ~ '_' ~ commande.id })}}
                {{ form_end(form, {render_rest: false}) }}

            </div>
        </div>
    </div>
{% endblock %}
