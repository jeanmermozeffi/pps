    {% block page_content %}
    <button type="button" class="btn btn-default btn-sm" data-toggle="modal" data-target="#modal-details{{commande.id}}"><i class="fa fa-list-alt"></i></button>
    <div class="modal fade" id="modal-details{{commande.id}}">

        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title">Details</h4>
                </div>
                <div class="modal-body">
                    <div class="form-group">
                        <label>Corporate</label>
                        <p class="help-block">{{ commande.corporate.raisonSociale }}</p>
                    </div>
                    <div class="form-group">
                        <label for="nom">Nombre de PASS</label>
                        <p class="help-block">{{ commande.qteCommande }}</p>
                    </div>
                    <div class="form-group">
                        <label for="nom">Date</label>
                        <p class="help-block">{{ commande.dateCommande|date('d-m-Y H:i:s') }}</p>
                    </div>
                    <div class="form-group">
                        <label for="nom">Détails</label>
                        <p class="help-block">{{ commande.detailsCommande }}</p>
                    </div>
                </div>

            </div>
        </div>
    </div>
    {% endblock %}
